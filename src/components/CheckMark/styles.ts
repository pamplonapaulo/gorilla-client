import styled from 'styled-components'

export const Svg = styled.svg`
  padding: 0;
  width: 50%;
  height: 100%;
  margin: 0;

  @media only screen and (min-width: 1024px) {
    float: right;
  }
`

export const Path = styled.path`
  opacity: 0.4;
`

export const PathSec = styled.path`
  opacity: 1;
`
